import { createContext } from 'react'

// the "CurrentUSerContext's value will be set on the App.js our main file

const CurrentUserContext = createContext(undefined)

export default CurrentUserContext